The V1 Recommendation Engine is rule-based.

Future versions may replace rule-based evaluation with evidence-weighted judgment.

The Recommendation Engine should therefore treat each Gate as a container of evidence rather than as a fixed score.